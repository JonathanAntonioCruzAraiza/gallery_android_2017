<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="utng.edu.mx.menusarahi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".registro.Registro" />
        <activity android:name=".gallery.BaseGuitarras" />
        <activity android:name=".gallery.BaseViolin" />
        <activity android:name=".gallery.BasePiano" />
        <activity android:name=".gallery.MenuGallery" />
        <activity android:name=".gallery.BaseSaxofon" />
        <activity android:name=".gallery.BaseChelo"/>
        <activity android:name=".gallery.BaseBateria"/>
    </application>

</manifest>